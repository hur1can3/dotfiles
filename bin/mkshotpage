#!/bin/bash
#
# pbrisbin 2009
#
###

. /home/hur1can3/.bin/siteutils

message() { echo 'usage: mkshotpage'; exit 1; }

# must be root
[ $(id -u) -ne 0 ] && errorout 'you must be root'

# find screenshots
build_sshots_array

# make the pages
mk_sshots_pages
